{
  "policy": {
    "bindings": [
      {
        "role": "roles/iap.egressor",
        "members": ["TRIP_PLANNER_AGENT_PRINCIPAL"],
        "condition": {
          "title": "AllowTripPlannerToHotelWithPermittedUsers",
          "description": "End user must be in mesh-full-user or mesh-hotel-user group.",
          "expression": "api.getAttribute('authorization.permissions', []).exists(p, p in ['mesh-full-user', 'mesh-hotel-user'])"
        }
      }
    ]
  }
}
